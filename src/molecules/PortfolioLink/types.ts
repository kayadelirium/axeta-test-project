import { IProps as ILinkProps } from "../../atoms/Link/types";
import { IProps as IImageProps } from "../../atoms/Image/types";
import { IPortfolio } from "../../types";

// export interface IProps extends ILinkProps, IImageProps {}

export interface IProps extends IPortfolio {}
